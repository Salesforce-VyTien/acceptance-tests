modules:
    config:
        Db:
            dsn: 'mysql:host=localhost;dbname=springboard'
            user: 'root'
            password: ''
            dump: ../dump.sql
            populate: true
            cleanup: true
extensions:
    config:
        Codeception\Extension\Phantoman:
           suites: ['acceptance (phantom)']